var canvas;
var gameState,contestant,contestantCount
var gameState = 0;
var playerCount;
function setup(){
  canvas = createCanvas(850,400);
  database = Firebase.Database
}


function draw(){
  background("pink");

  
}
